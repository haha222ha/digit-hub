# xhs-cloud（测评 + 支付内核）

从本地 `vuemonitor/xhs-cloud/cloud_deploy` 同步，含 `GET /api/v1/assess/access` 与 assess 套餐。

主机安装：

```bash
sudo bash /opt/digit-hub/deploy/api_fresh_install.sh
```

运行目录：`/opt/xhs-cloud`（venv + `.env` + `cloud_deploy/`）。

**勿**将 `.env` 或商户密钥提交到本仓库。
